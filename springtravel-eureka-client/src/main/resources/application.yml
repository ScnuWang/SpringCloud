server:
  port: 8082
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8091/eureka
spring:
  application:
    name: eureka-client

management:
  endpoints:
    web:
      exposure:
        include: "*"
      cors:
        allowed-origins: "*"
        allowed-methods: "*"

